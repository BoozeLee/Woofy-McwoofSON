# 🐶 API Endpoints

Document all API endpoints, methods, parameters, authentication, and example calls here.

## Example

- **GET /api/v1/fetch**
  - Description: Fetches a document
  - Auth: Required (JWT)
  - Params: `doc_id` (string)
  - Response: `{ ... }`
